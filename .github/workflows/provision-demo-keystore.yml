name: Provision demo keystore

# One-shot generator for keystore/gatekeeper-demo.keystore (run manually,
# download the artifact, commit it on a branch). After that every CI build
# signs with the same certificate so the demo APK updates in place.
on:
  workflow_dispatch: {}

jobs:
  provision:
    name: provision
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up JDK 17
        uses: 1337farm/actions/setup-jdk@v1
        with:
          java-version: "17"

      - name: Generate demo keystore
        run: |
          rm -f gatekeeper-demo.keystore
          keytool -genkeypair \
            -keystore gatekeeper-demo.keystore \
            -alias demo -keyalg RSA -keysize 2048 -validity 9125 \
            -storepass gatekeeper -keypass gatekeeper \
            -dname "CN=ForgeGatekeeper Demo, OU=Demo, O=1337farm"
          keytool -list -keystore gatekeeper-demo.keystore \
            -storepass gatekeeper | head -5

      - name: Upload keystore artifact
        uses: actions/upload-artifact@v6
        with:
          name: demo-keystore
          path: gatekeeper-demo.keystore
          retention-days: 7
